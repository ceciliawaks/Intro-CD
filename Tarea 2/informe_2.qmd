---
title: "Entrega 2 - Introducción a la Ciencia de Datos"
author: "Grupo 8: Facundo Morini, Cecilia Waksman"
lang: es
format:
  pdf:
    titlepage: true
    include-in-header:
      text: |
        \usepackage{booktabs}
        \usepackage{float}
        \usepackage{titling}
        \setlength{\droptitle}{-2.5cm}
        \posttitle{\par\end{center}\vspace{-0.8em}}
execute:
    echo: false
    warning: false
    message: false
---

```{r}
library(reticulate)

# use_python(
#    "C:/Users/facur/anaconda3/envs/fertcast/python.exe",
#    required = TRUE
#  )

Sys.setenv(RETICULATE_PYTHON = here::here(".venv/bin/python"))

```

```{python}
from time import time
from pathlib import Path

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import re
import string

import prince
from adjustText import adjust_text

from datasets import load_dataset
from sklearn.model_selection import train_test_split, GridSearchCV, cross_val_score, StratifiedKFold
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer, ENGLISH_STOP_WORDS
from sklearn.decomposition import PCA, TruncatedSVD
from sklearn.naive_bayes import MultinomialNB
from sklearn.linear_model import LogisticRegression
from sklearn.multiclass import OneVsRestClassifier
from sklearn.metrics import (
    accuracy_score,
    confusion_matrix,
    ConfusionMatrixDisplay,
    classification_report,
)
```

```{python}
ds = load_dataset("tomas-gr/all-the-news-2-1-Component-one-sampled", split="train", cache_dir="data")
df = ds.to_pandas()

df["date"] = pd.to_datetime(df["date"], format="mixed")
df["year"] = pd.to_numeric(df["year"], errors="coerce")
df["month"] = pd.to_numeric(df["month"], errors="coerce")
df["day"] = pd.to_numeric(df["day"], errors="coerce")

text_columns = [
    "author",
    "title",
    "article",
    "url",
    "section",
    "publication"
]

df[text_columns] = df[text_columns].astype("string")
```

```{python}
#| include: false
# Pipeline reutilizado de la Tarea 1 para reconstruir las dos figuras
# introductorias (top de medios y análisis de correspondencias).


def clean_text(df, column_name):

    # Eliminar encabezado hasta el primer salto de línea
    result = df[column_name].str.replace(r"^[^\n]*\n", "", regex=True)

    # Minúsculas
    result = result.str.lower()

    # Reemplazar puntuación y saltos de línea por espacios
    punctuation = re.escape(string.punctuation + "—–¿¡«»")
    result = result.str.replace(f"[{punctuation}\\n]", " ", regex=True)

    # Eliminar palabras que contengan caracteres no permitidos
    result = result.str.replace(
        r"\b\S*[^a-zA-Z\sáéíóúüñ]\S*\b", "", regex=True
    )

    # Eliminar espacios múltiples
    result = result.str.replace(r"\s+", " ", regex=True).str.strip()

    return result


# Cinco medios con mayor cantidad de artículos (los usados en la Tarea 1)
top_5_publications = df["publication"].value_counts().head(5).index
df_top_5 = df[df["publication"].isin(top_5_publications)].copy()

df_top_5["CleanText"] = clean_text(df_top_5, "article")

stopwords = list(ENGLISH_STOP_WORDS) + ["s", "u", "say", "said"]
df_top_5["CleanText_no_stop_words"] = (
    df_top_5["CleanText"]
    .str.replace(r"\b(" + "|".join(stopwords) + r")\b", "", regex=True)
    .str.replace(r"\s+", " ", regex=True)
    .str.strip()
)

# Tabla medios x palabras para el análisis de correspondencias
rows = []
top_n = 100
for publication, group in df_top_5.groupby("publication"):
    texts = group["CleanText_no_stop_words"].dropna().astype(str);
    vectorizer = CountVectorizer();
    word_matrix = vectorizer.fit_transform(texts);
    word_counts = word_matrix.sum(axis=0).A1
    words = vectorizer.get_feature_names_out();
    freq_df = pd.DataFrame(
        {"publication": publication, "word": words, "count": word_counts}
    );
    rows.append(freq_df.sort_values("count", ascending=False).head(top_n));

top_words_df = pd.concat(rows, ignore_index=True);

other_stopwords = {
    "new", "times", "ms",
    "cnbc", "reuters", "nyt", "newyorktimes", "york",
}
stopwords_ca = set(stopwords).union(other_stopwords);
df_ca = top_words_df[~top_words_df["word"].isin(stopwords_ca)].copy();

top_100_words = (
    df_ca.groupby("word")["count"]
    .sum()
    .sort_values(ascending=False)
    .head(100)
    .index
);
df_ca = df_ca[df_ca["word"].isin(top_100_words)];

tabla_ca = df_ca.pivot_table(
    index="publication",
    columns="word",
    values="count",
    aggfunc="sum",
    fill_value=0,
);
```

\vspace{-1.25cm} En este trabajo se busca construir un modelo de clasificación de texto capaz de predecir a qué medio de prensa pertenece un artículo periodístico a partir únicamente de su contenido textual. Para esto se utiliza un subconjunto del dataset *All the News 2.1 -- Component One*, que reúne artículos de distintos medios junto con los metadatos asociados a cada publicación. 

El análisis exploratorio que se realizó previamente sobre estos mismos datos ya había dejado algunas señales útiles para esta etapa. La primera es el fuerte desbalance en la cantidad de artículos entre medios (Figura \ref{fig-top-medios}): \texttt{Reuters} concentra una porción muy superior al resto, algo que conviene tener presente tanto al entrenar los modelos como, sobre todo, al interpretar sus métricas. La segunda es que los propios artículos suelen incluir pistas directas de su origen, como menciones al nombre del medio, direcciones web o firmas editoriales, que de no removerse podrían facilitar la tarea de forma artificial. La tercera es que, aunque los medios comparten temáticas transversales como la política estadounidense, mostraban perfiles de vocabulario diferenciados. Esto último quedaba resumido en el análisis de correspondencias entre medios y términos frecuentes (Figura \ref{fig-ca}): \texttt{Reuters} y \texttt{CNBC} aparecían próximos entre sí y ligados a un lenguaje económico e internacional, mientras que \texttt{The New York Times} se inclinaba hacia la política y hacia temas más cercanos a la vida cotidiana y la cultura. Estas diferencias sugieren, ya de entrada, que habrá medios más fáciles de distinguir que otros.

::: {layout-ncol=2}
```{python}
#| label: fig-top-medios
#| fig-cap: "Diez medios con mayor cantidad de artículos en la base."
#| fig-align: center
#| fig-pos: "H"

conteo_medios = df["publication"].dropna().value_counts();
top_10_medios = conteo_medios.head(10);

fig, ax = plt.subplots(figsize=(7, 5));
top_10_medios.sort_values().plot(kind="barh", ax=ax);
ax.set_xlabel("Cantidad de artículos");
ax.set_ylabel("Medio de prensa");
ax.set_title("Top 10 medios con más artículos");
ax.set_xlim(0, 10500);

for i, value in enumerate(top_10_medios.sort_values()):
    ax.text(value, i, f" {value}", va="center", fontsize=9);

ax.grid(axis="x", alpha=0.3);
plt.tight_layout();
plt.show();
```

```{python}
#| label: fig-ca
#| fig-cap: "Análisis de correspondencias entre los cinco medios principales y las palabras más frecuentes."
#| fig-align: center
#| fig-pos: "H"

ca = prince.CA(n_components=2, n_iter=100, random_state=123);
ca = ca.fit(tabla_ca);

row_coords = ca.row_coordinates(tabla_ca);
col_coords = ca.column_coordinates(tabla_ca);

palabras = [
    "democrats", "senate", "capitol", "dc", "washington", "campaign",
    "house", "trump", "market", "markets", "financial", "bank",
    "billion", "shares", "investors", "oil", "economy", "trade",
    "family", "life", "like", "work", "american", "political", "white", "mr",
];
palabras_mostrar = [p for p in palabras if p in col_coords.index];
palabras_no_mostrar = [p for p in col_coords.index if p not in palabras];

fig, ax = plt.subplots(figsize=(8, 7));
ax.scatter(
    col_coords.loc[palabras_no_mostrar, 0],
    col_coords.loc[palabras_no_mostrar, 1],
    color="tab:blue", alpha=0.30, s=25,
);
ax.scatter(
    col_coords.loc[palabras_mostrar, 0],
    col_coords.loc[palabras_mostrar, 1],
    color="tab:orange", alpha=0.9, s=55,
);

texts = [
    ax.text(
        col_coords.loc[p, 0], col_coords.loc[p, 1], p,
        fontsize=11, color="tab:orange",
    )
    for p in palabras_mostrar
];

ax.scatter(row_coords[0], row_coords[1], color="black", s=90, label="Medios");

offset_medios = {
    "The New York Times": (0.05, 0.06),
    "People": (-0.06, -0.08),
    "CNBC": (0.02, 0.03),
    "Reuters": (0.02, -0.06),
    "The Hill": (0.02, 0.03),
}
for medio in row_coords.index:
    dx, dy = offset_medios.get(medio, (0.02, 0.02));
    ax.text(
        row_coords.loc[medio, 0] + dx,
        row_coords.loc[medio, 1] + dy,
        medio, fontsize=12, fontweight="bold", color="black",
        ha="right" if dx < 0 else "left", va="center",
    );

import io, contextlib

with contextlib.redirect_stdout(io.StringIO()):
    adjust_text(
        texts, ax=ax,
        force_text=(0.05, 0.08), force_static=(0.05, 0.08),
        force_pull=(0.01, 0.02), max_move=(8, 8),
        expand=(1.05, 1.10), ensure_inside_axes=True,
        arrowprops=dict(arrowstyle="-", lw=0.4, alpha=0.4, color="gray"),
    );

ax.axhline(0, linestyle="--", linewidth=0.8);
ax.axvline(0, linestyle="--", linewidth=0.8);
ax.set_xlabel("Dimensión 1");
ax.set_ylabel("Dimensión 2");
ax.set_title("Análisis de correspondencias: medios y términos frecuentes");
ax.legend();
ax.grid(alpha=0.3);
plt.tight_layout();
plt.show();
```
:::

Para esta tarea primero se traducirá el texto a una representación numérica con la que un modelo pueda operar, explorando técnicas como *bag of words* y *TF-IDF*, y observando mediante una reducción de dimensionalidad hasta qué punto los medios resultan separables. Luego se entrena y evalúa el rendimiento de modelos como *Multinomial Naive Bayes*, midiendo su desempeño con métricas de clasificación.

# Dataset y representación numérica de texto

## 1. Preparación de los datos

Para esta tarea el análisis se restringe a los tres medios con mayor cantidad de artículos de la base, ya identificados: \texttt{Reuters}, \texttt{The New York Times} y \texttt{CNBC} (Figura \ref{fig-top-medios}). Una vez descartados los artículos sin cuerpo de texto, estos tres medios reúnen 14.660 artículos, sobre los que se construye el conjunto de trabajo.

Sobre el cuerpo de cada artículo (variable \texttt{article}) se aplica una misma función, de modo de homogeneizar el texto antes de convertirlo en una representación numérica. Esta función elimina el encabezado que precede al primer salto de línea, convierte el texto a minúsculas, reemplaza los signos de puntuación y los saltos de línea por espacios, descarta las palabras que contienen caracteres no alfabéticos (restos de enlaces, números o códigos) y, finalmente, colapsa los espacios múltiples en uno solo.

```{python}
# La función clean_text fue definida en el bloque de preparación inicial.

top_3_publications = df["publication"].value_counts().head(3).reset_index()
top_3_publications.columns = [
    "Medio de prensa",
    "Cantidad de artículos"]

df_top_3 = (
    df[df["publication"].isin(top_3_publications["Medio de prensa"])]
    .copy()
)

df_top_3["CleanText"] = clean_text(df_top_3, "article")

df_top_3 = df_top_3.dropna(
    subset=["CleanText", "publication"]
).copy()

```

A partir de este conjunto se separa un 30\% de los artículos para conformar el conjunto de test, reservando el 70\% restante para el entrenamiento. Dado el fuerte desbalance entre medios: \texttt{Reuters} concentra cerca del 63\% de los artículos, frente a un 19\% de \texttt{The New York Times} y un 18\% de \texttt{CNBC}, la partición se realiza mediante **muestreo estratificado**. De esta forma se garantiza que cada medio conserve aproximadamente la misma proporción en ambos subconjuntos, evitando que, por azar, alguno de los medios minoritarios quede sub o sobre-representado en el conjunto de test. Como resultado, el conjunto de entrenamiento queda conformado por 10.262 artículos y el de test por 4.398.

```{python}
# Partición train/test (30% test) con muestreo estratificado por medio
X = df_top_3["CleanText"]
y = df_top_3["publication"]


X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=33, stratify=y)

#print(X_train.shape) #10262
#print(X_test.shape) #4398

```

Para verificar que la estratificación efectivamente preservó el balance original, en la Figura \ref{fig-balance-clases} se compara la proporción de artículos de cada medio en el conjunto total y en los conjuntos de entrenamiento y test.

```{python}
#| label: fig-balance-clases
#| fig-cap: "Proporción de artículos de cada medio en el conjunto total, de entrenamiento y de test."
#| fig-align: center
#| fig-pos: "H"

orden_medios = y.value_counts().index;

dist_df = pd.concat(
    [
        y.value_counts(normalize=True),
        y_train.value_counts(normalize=True),
        y_test.value_counts(normalize=True),
    ],
    axis=1,
);
dist_df.columns = ["Total", "Train", "Test"];
dist_df = dist_df.loc[orden_medios] * 100

fig, ax = plt.subplots(figsize=(9, 5));
dist_df.plot(
    kind="bar",
    ax=ax,
    width=0.8,
    color=["#4c72b0", "#dd8452", "#55a868"],
);

ax.set_xlabel("Medio de prensa", fontsize=12);
ax.set_ylabel("Porcentaje de artículos", fontsize=12);
ax.set_title("Balance de clases en train y test", fontsize=14, pad=12);
ax.set_ylim(0, dist_df.values.max() * 1.18);
ax.tick_params(axis="x", labelrotation=0, labelsize=11);
ax.grid(axis="y", alpha=0.3);
ax.legend(title="Conjunto", fontsize=10, title_fontsize=11);

for contenedor in ax.containers:
    ax.bar_label(contenedor, fmt="%.1f%%", padding=2, fontsize=8);

plt.tight_layout()
plt.show()
```

Las proporciones resultan prácticamente idénticas en los tres conjuntos: las diferencias entre train y test no superan la centésima de punto porcentual para ningún medio. De todas formas, la figura vuelve a dejar en evidencia el marcado desbalance entre clases, con \texttt{Reuters} representando casi dos tercios del total. Este desbalance no se corrige, sino que se mantiene de forma deliberada para trabajar sobre los datos tal como se presentan, pero conviene tenerlo presente de cara a la evaluación de los modelos, ya que condiciona la interpretación de métricas como la \textit{accuracy}, según se discutirá posteriormente.

## 2. Representación Bag of Words

La técnica de *bag of words* (BOW) traduce cada artículo a un vector de conteos de palabras. Concretamente, se construye una matriz en la que cada fila corresponde a un artículo y cada columna a una de las palabras distintas presentes en el conjunto de entrenamiento; el valor de cada celda es la cantidad de veces que esa palabra aparece en ese artículo. El nombre de la técnica refleja que se descarta el orden de las palabras: solo importa cuántas veces aparece cada una, y no su posición ni el contexto en que se encuentra.

```{python}
count_vect = CountVectorizer()
X_train_bow = count_vect.fit_transform(X_train)

# X_train_bow.shape  # (10262, 82287)
```

```{python}
# promedio_palabras = (
#     df_top_3["CleanText"]
#     .dropna()
#     .str.split()
#     .str.len()
#     .mean()
# )

# print(promedio_palabras)#424

# cant_palabras = (
#     df_top_3["CleanText"]
#     .dropna()
#     .str.split()
#     .str.len()
# )
# 
# print(cant_palabras.max()) # 12069
```


Aplicada sobre el conjunto de entrenamiento, esta transformación produce una matriz de dimensiones $(10262, 82287)$: 10.262 artículos por las 82.287 palabras distintas que conforman el vocabulario.

A modo de ejemplo, en el Cuadro \ref{tab:ejemplo_bow} se muestran los conteos de seis palabras del primer artículo, para los cinco primeros artículos del conjunto de entrenamiento. Se eligieron columnas con al menos un valor distinto de cero.

```{python}
#| results: asis

bow_names = count_vect.get_feature_names_out()

fila = 0

# Columnas con valores distintos de cero
columnas_bow = X_train_bow[fila].nonzero()[1][:6]

ejemplo_bow = pd.DataFrame(
    X_train_bow[:5][:, columnas_bow].toarray(),
    columns=bow_names[columnas_bow]
)

tabla_bow=ejemplo_bow.to_latex(
    index=False,
    escape=True,
    caption="Ejemplo de representación Bag of Words.",
    label="tab:ejemplo_bow"
)
tabla_bow = tabla_bow.replace(
    "\\begin{table}",
    "\\begin{table}[H]\n\\centering\n\\scriptsize"
)

print(tabla_bow)

```


El ejemplo ya anticipa una característica central de esta representación: la enorme mayoría de las celdas valen cero. Cada artículo utiliza apenas una pequeña fracción del vocabulario total; en este subconjunto, el promedio de palabras por artículo es de aproximadamente $424 \ll 82287$, muy por debajo del número de columnas. Incluso el artículo más extenso, con $12069 < 82287$ palabras, está lejos de cubrir el vocabulario completo, de modo que la fila que le corresponde sigue estando dominada por ceros. Por este motivo se dice que la matriz es dispersa.

Por último, vale señalar que esta representación es generalizable: en lugar de contar palabras sueltas, puede contarse la frecuencia de cada secuencia de $n$ palabras consecutivas —un **n-grama**, es decir, una secuencia de $n$ elementos contiguos—, en lo que se conoce como *bag of n-grams*. Vista así, BOW no es más que el caso particular con $n=1$.

## 3. Representación TF-IDF

La representación *Term Frequency – Inverse Document Frequency* (TF-IDF) aborda una limitación de los conteos absolutos de BOW: dependen del largo del texto, ya que un artículo más extenso tenderá a repetir más veces cada palabra. El primer componente, *Term Frequency* (TF), corrige esto reemplazando el conteo por una frecuencia relativa al largo del texto:

$$TF_{t,p}=\frac{\text{conteo de la palabra } p \text{ en el texto } t}{\text{conteo total de palabras en el texto } t}$$

El segundo componente, *Inverse Document Frequency* (IDF), pondera cada palabra según qué tan extendida está en el conjunto de textos. Se define como el logaritmo del cociente entre la cantidad total de textos y la cantidad de textos que contienen la palabra:

$$IDF_{p}=\log\!\left(\frac{\text{cantidad total de textos}}{\text{cantidad de textos que contienen la palabra } p}\right)$$

De esta forma, las palabras que aparecen en muchos textos —y que, por lo tanto, son poco distintivas, como _the_, _and_ o _a_— reciben un peso bajo, mientras que las que aparecen en pocos textos, presumiblemente más informativas del contenido, reciben un peso alto.

La representación final se obtiene multiplicando ambos componentes:

$$TF\text{-}IDF_{t,p} = TF_{t,p} \times IDF_{p}$$

Reutilizando las mismas palabras del ejemplo anterior, el Cuadro \ref{tab:ejemplo_tfidf} muestra ahora su representación TF-IDF.

```{python}
#| results: asis

tfidf_vect = TfidfVectorizer()
X_train_tfidf = tfidf_vect.fit_transform(X_train)

# X_train_tfidf.shape  # (10262, 82287)

tfidf_names = tfidf_vect.get_feature_names_out()

# Elegir 6 palabras presentes en el primer documento
columnas_tfidf = X_train_tfidf[0].nonzero()[1][:6]

ejemplo_tfidf = pd.DataFrame(
    X_train_tfidf[:5][:, columnas_tfidf].toarray(),
    columns=tfidf_names[columnas_tfidf]
).round(4)

tabla_tfidf=ejemplo_tfidf.to_latex(
    index=False,
    escape=True,
    float_format="%.4f",
    caption="Ejemplo de representación TF-IDF.",
    label="tab:ejemplo_tfidf"
)
tabla_tfidf = tabla_tfidf.replace(
    "\\begin{table}",
    "\\begin{table}[H]\n\\centering\n\\scriptsize"
)

print(tabla_tfidf)

```

En el primer artículo, la mayoría de las palabras del ejemplo toman valores parecidos, en torno a $0.12$–$0.13$. La excepción es \texttt{reuters}, con un valor mucho menor ($0.01$), y esto se explica por los dos componentes a la vez. Por un lado, su _TF_ es bajo: en ese artículo \texttt{reuters} aparece una sola vez, frente a las cuatro apariciones de palabras como \texttt{los} o \texttt{star}. Por otro, y sobre todo, su _IDF_ también es bajo: \texttt{reuters} figura en una gran cantidad de artículos, algo ya previamente visto, donde \texttt{Reuters} resultó ser el medio con más artículos y el que más se menciona a sí mismo. Al estar tan extendida, la palabra aporta poca información para distinguir un artículo de otro, y la transformación la penaliza en consecuencia.

## 4. Visualización PCA

Una vez transformado el conjunto de datos a las técnicas descritas, podemos explorar la estructura de los datos mediante una reducción de dimensionalidad. Para esto se proyecta el conjunto de entrenamiento sobre las dos primeras componentes principales de los vectores TF-IDF, para observar hasta qué punto los medios resultan separables y analizar cómo cambia esa proyección al variar la representación.

Para esto se evaluó el efecto de tres variantes distintas: (a) eliminar las _stop words_ del inglés, palabras vacías estándar más \texttt{s}, \texttt{u}, \texttt{say} y \texttt{said}—; (b) desactivar el IDF quedándonos solo con la frecuencia de término; y (c) incorporar bigramas además de palabras sueltas.

Dado que se esta trabajando con una matriz dispersa no es recomendable aplicar PCA directamente, ya que requiere convertir la matriz en una matriz densa de dimensiones demasiado grandes. Por eso, en lugar de PCA, se utiliza la descomposición SVD truncada, que puede operar directamente sobre la matriz dispersa al no necesitar centrar los datos. Salvo por ese centrado, ambas técnicas son equivalentes.

```{python}
# SVD
svd = TruncatedSVD(
    n_components=10,
    random_state=33
)

X_svd = svd.fit_transform(X_train_tfidf)

# X_svd.shape #(10262, 2)

df_svd = pd.DataFrame({
    "PC1": X_svd[:, 0],
    "PC2": X_svd[:, 1],
    "publication": y_train.values
})
```

```{python}

#stopwords
stopwords = list(ENGLISH_STOP_WORDS) + ["s", "u", "say", "said"]

tfidf_stop = TfidfVectorizer(
    stop_words=stopwords
)

X_train_stop = tfidf_stop.fit_transform(X_train)

#X_train_stop.shape #(10262, 2)

# SVD
svd_stop = TruncatedSVD(
    n_components=10,
    random_state=33
)
X_svd_stop = svd_stop.fit_transform(
    X_train_stop
)

df_svd_stop = pd.DataFrame({
    "PC1": X_svd_stop[:, 0],
    "PC2": X_svd_stop[:, 1],
    "publication": y_train.values
})

```

```{python}
# (b) Sin IDF: solo frecuencia de término
tfidf_tf = TfidfVectorizer(use_idf=False)
X_train_tf = tfidf_tf.fit_transform(X_train)

svd_tf = TruncatedSVD(n_components=10, random_state=33)
X_svd_tf = svd_tf.fit_transform(X_train_tf)

df_svd_tf = pd.DataFrame({
    "PC1": X_svd_tf[:, 0],
    "PC2": X_svd_tf[:, 1],
    "publication": y_train.values,
})

# (c) Unigramas + bigramas
tfidf_bigram = TfidfVectorizer(ngram_range=(1, 2))
X_train_bigram = tfidf_bigram.fit_transform(X_train)

svd_bigram = TruncatedSVD(n_components=10, random_state=33)
X_svd_bigram = svd_bigram.fit_transform(X_train_bigram)

df_svd_bigram = pd.DataFrame({
    "PC1": X_svd_bigram[:, 0],
    "PC2": X_svd_bigram[:, 1],
    "publication": y_train.values,
})
```


```{python}
#| label: fig-svd-comparacion
#| fig-cap: "Proyección sobre las dos primeras componentes SVD del conjunto de entrenamiento: TF-IDF base y las tres variantes de la consigna."
#| fig-align: center
#| fig-pos: "H"

from matplotlib.lines import Line2D

colores_medios = {
    "Reuters": "tab:orange",
    "CNBC": "tab:blue",
    "The New York Times": "tab:green",
}

casos = [
    ("TF-IDF base", df_svd),
    ("(a) Stop words", df_svd_stop),
    ("(b) Sin IDF (use_idf=False)", df_svd_tf),
    ("(c) Unigramas + bigramas", df_svd_bigram),
]

fig, axs = plt.subplots(2, 2, figsize=(10, 8))

for ax, (titulo, df_plot) in zip(axs.ravel(), casos):
    for medio, color in colores_medios.items():
        subset = df_plot[df_plot["publication"] == medio]
        ax.scatter(
            subset["PC1"], subset["PC2"],
            s=10, alpha=0.12, color=color
        )
    ax.set_title(titulo, fontsize=10, pad=4)
    ax.tick_params(axis="both", labelsize=8)

fig.supxlabel("Componente 1", fontsize=10, y=0.04)
fig.supylabel("Componente 2", fontsize=10, x=0.04)

handles = [
    Line2D([0], [0], marker="o", linestyle="", color=color, label=medio)
    for medio, color in colores_medios.items()
]
fig.legend(
    handles=handles, loc="lower center", ncol=3,
    fontsize=8, frameon=True, bbox_to_anchor=(0.5, -0.01)
)

plt.tight_layout(rect=[0.04, 0.05, 1, 0.97])
plt.show()
```

En los cuatro casos los medios aparecen fuertemente solapados: ninguna de las representaciones logra separarlos de forma limpia en dos dimensiones. Aun así, las variantes se comportan de manera muy distinta. El TF-IDF base y la versión con _stop words_ producen graficos con forma similar, en los que se insinúa cierta organización: \texttt{Reuters} y \texttt{The New York Times} tienden a ocupar regiones algo diferentes, con \texttt{CNBC} repartido entre ambas, aunque con mucho solapamiento. Quitar las _stop words_ no cambia demasiado el aspecto del mapa, pero sí reduce la varianza que capturan las primeras componentes (de un 3,9\% a un 1,5\% en las dos primeras).

El cambio más marcado aparece al desactivar el IDF (\texttt{use\_idf=False}). En ese caso las dos primeras componentes concentran mucha más varianza (alrededor del 16\%), pero esa varianza no separa a los medios: el primer plano principal se ordena a lo largo de un eje dominado por las palabras más frecuentes —comunes a todos los medios—, y las clases quedan aún más mezcladas. Esto ilustra el rol del IDF: al penalizar los términos ubicuos, evita que la representación quede gobernada por vocabulario genérico y deja lugar a las diferencias entre medios.

Incorporar bigramas (\texttt{ngram\_range=(1,2)}), por su parte, multiplica el tamaño del vocabulario de unas 82.000 columnas a más de 1,4 millones pero apenas modifica la proyección, que se mantiene muy parecida a la de unigramas. 

```{python}
#| label: fig-varianza-svd
#| fig-cap: "Varianza explicada acumulada por las primeras diez componentes SVD."
#| fig-align: center
#| fig-pos: "H"

plt.figure(figsize=(8, 5))

for nombre, modelo in [
    ("TF-IDF base", svd),
    ("(a) Stop words", svd_stop),
    ("(b) Sin IDF", svd_tf),
    ("(c) Bigramas", svd_bigram),
]:
    plt.plot(
        range(1, 11),
        np.cumsum(modelo.explained_variance_ratio_),
        marker="o",
        label=nombre,
    )

plt.xlabel("Número de componentes")
plt.ylabel("Varianza explicada acumulada")
plt.title("Varianza explicada acumulada por SVD")
plt.grid(True)
plt.legend()

plt.show()
```

La Figura \ref{fig-varianza-svd} confirma la dificultad: en las representaciones con IDF, las dos primeras componentes explican menos del 4\% de la varianza total y, aun con diez componentes, no se supera el 8\%. Por lo tanto no es posible separar a los medios usando solo dos componentes principales, ya que la información que los distingue está repartida en muchas direcciones del espacio.

Las dos primeras componentes, de todos modos, dejan entrever cierta estructura. Para entender si responde a diferencias temáticas, aprovechamos que el propio conjunto de datos ya trae un área temática etiquetada en la variable \texttt{section}. En el Cuadro \ref{tab:top-secciones} se listan las cinco secciones más frecuentes de cada medio, que reflejan con bastante claridad su perfil editorial: \texttt{Reuters} se concentra en secciones financieras y de mercados, \texttt{The New York Times} reparte su cobertura entre noticias, opinión, cultura y deportes, y \texttt{CNBC} mezcla negocios, tecnología y política.

```{python}
#| results: asis

top_secciones = {}
for medio in ["Reuters", "The New York Times", "CNBC"]:
    vc = (
        df_top_3.loc[df_top_3["publication"] == medio, "section"]
        .value_counts(dropna=True)
    )
    total = vc.sum()
    top_secciones[medio] = [
        f"{nombre} ({100 * n / total:.1f}%)"
        for nombre, n in vc.head(5).items()
    ]

tabla_sec = pd.DataFrame(top_secciones)
tabla_sec.index = [1, 2, 3, 4, 5]

latex_sec = tabla_sec.to_latex(
    index=True,
    escape=True,
    caption="Cinco secciones más frecuentes en cada medio (porcentaje sobre los artículos con sección registrada).",
    label="tab:top-secciones",
    column_format="clll"
)
latex_sec = latex_sec.replace(
    "\\begin{table}",
    "\\begin{table}[H]\n\\centering\n\\scriptsize"
)
print(latex_sec)
```

En el caso de \texttt{CNBC}, la etiqueta más frecuente es \emph{Wires}, que agrupa despachos de agencia y no constituye una temática en sí, por lo que se omite del gráfico que sigue. A partir de ellas tomamos algunas secciones representativas y, sobre el mismo espacio SVD del TF-IDF con stop words, calculamos su centro de gravedad como la posición media de los artículos de entrenamiento que pertenecen a cada sección (Figura \ref{fig-centroides}).

```{python}
#| label: fig-centroides
#| fig-cap: "Centros de gravedad de cada medio y de sus secciones más representativas, sobre las dos primeras componentes SVD del TF-IDF con stop words."
#| fig-align: center
#| fig-pos: "H"

df_svd_stop["section"] = df_top_3.loc[X_train.index, "section"].values

colores = {
    "Reuters": "tab:orange",
    "CNBC": "tab:blue",
    "The New York Times": "tab:green"
}

secciones = ["Markets", "World News", "opinion", "arts", "sports"]

med_cent = df_svd_stop.groupby("publication")[["PC1", "PC2"]].mean()

sec_cent = {
    s: df_svd_stop.loc[df_svd_stop["section"] == s, ["PC1", "PC2"]].mean()
    for s in secciones
}

med_off = {
    "Reuters": (0, 13, "center"),
    "CNBC": (13, 2, "left"),
    "The New York Times": (0, -16, "center")
}
sec_off = {
    "Markets": (8, 4, "left"),
    "World News": (8, 5, "left"),
    "opinion": (8, -10, "left"),
    "arts": (-8, -12, "right"),
    "sports": (-8, 10, "right")
}

fig, ax = plt.subplots(figsize=(9, 6.5))

for medio in colores:
    sub = df_svd_stop[df_svd_stop["publication"] == medio]
    ax.scatter(sub["PC1"], sub["PC2"], s=8, alpha=0.05, color=colores[medio])

for medio in colores:
    ax.scatter(
        med_cent.loc[medio, "PC1"], med_cent.loc[medio, "PC2"],
        s=260, marker="o", color=colores[medio],
        edgecolor="black", linewidth=1.5, zorder=5
    )
    dx, dy, ha = med_off[medio]
    ax.annotate(
        medio, (med_cent.loc[medio, "PC1"], med_cent.loc[medio, "PC2"]),
        fontsize=12, fontweight="bold", zorder=6,
        xytext=(dx, dy), textcoords="offset points", ha=ha
    )

for s, c in sec_cent.items():
    ax.scatter(
        c["PC1"], c["PC2"], s=90, marker="s",
        color="gray", edgecolor="black", linewidth=0.6, zorder=7
    )
    dx, dy, ha = sec_off[s]
    ax.annotate(
        s, (c["PC1"], c["PC2"]),
        fontsize=9, color="dimgray", zorder=8,
        xytext=(dx, dy), textcoords="offset points", ha=ha
    )

ax.axhline(0, ls="--", lw=0.6, color="gray", alpha=0.5)
ax.set_xlabel("Componente 1")
ax.set_ylabel("Componente 2")
ax.grid(alpha=0.2)
plt.tight_layout()
plt.show()
```

La figura confirma la lectura temática. La sección de mercados (*Markets*) se ubica en la franja de negocios que comparten \texttt{Reuters} y \texttt{CNBC}, mientras que en el extremo opuesto se agrupan las secciones más editoriales y culturales de \texttt{The New York Times} (*opinion*, *arts*, *sports*), que arrastran su centro de gravedad hacia abajo.

# Parte 2: Entrenamiento y Evaluación de Modelos

## 1. Multinomial Naive Bayes

A diferencia de la Parte 1, donde se trabajó con el TF-IDF base, para entrenar los modelos partimos de una representación que elimina las *stop words* y, además, las **referencias directas a los medios**: su propio nombre, el de su empresa matriz y los dominios web. Esta decisión está motivada por las pistas detectadas en la Tarea 1, y busca evitar que el clasificador aproveche menciones explícitas del propio medio en lugar de aprender de su contenido.

Se entrena entonces un modelo Multinomial Naive Bayes base, el cual define sus parámetros con valores por defecto de la función _MultinomialNB_, por lo que en este caso el parámetro *alpha* toma el valor 1 (en secciones siguientes se profundiza sobre este parámetro).

Se obtiene el *accuracy* del modelo y distintas medidas del desempeño en las predicciones para cada uno de los medios de prensa y presentamos la respectiva matriz de confusión.

$$\text{accuracy}=\frac{\text{Cantidad de predicciones correctas}}{\text{Cantidad ttal de predicciones}}$$

```{python}
# Features para los modelos: TF-IDF eliminando stop words y referencias directas a los medios
stopwords_modelo = list(ENGLISH_STOP_WORDS) + ["s", "u", "say", "said"]
referencias_medios = [
    "reuters", "cnbc", "nyt", "nytimes", "newyorktimes",
    "new", "york", "times", "com", "eikon", "thomson"
]
stopwords_modelo = stopwords_modelo + referencias_medios

tfidf_model = TfidfVectorizer(stop_words=stopwords_modelo)
X_train_model = tfidf_model.fit_transform(X_train)
X_test_model = tfidf_model.transform(X_test)
```

```{python}
# Entrenamiento del modelo y predicción sobre el conjunto de test
mnb = MultinomialNB();
mnb.fit(X_train_model, y_train);

y_pred_mnb = mnb.predict(X_test_model);

acc_mnb = accuracy_score(y_test, y_pred_mnb);
#print(f"Accuracy en test: {acc_mnb:.3f}")
```

```{python}
#| label: fig-cm-mnb
#| fig-cap: "Matriz de confusión del modelo Multinomial Naive Bayes sobre el conjunto de test."
#| fig-align: center
#| fig-pos: "H"

# Matriz de confusión en cantidades absolutas
cm_abs = confusion_matrix(
    y_test,
    y_pred_mnb,
    labels=mnb.classes_
);

# Normalización por fila:
# cada fila representa el 100% de los artículos reales de ese medio
cm_pct = cm_abs / cm_abs.sum(axis=1, keepdims=True) * 100;

# Texto de cada celda: cantidad absoluta y porcentaje
annot = np.empty_like(cm_abs).astype(str);

for i in range(cm_abs.shape[0]):
    for j in range(cm_abs.shape[1]):
        annot[i, j] = f"{cm_abs[i, j]}\n({cm_pct[i, j]:.1f}%)"

fig, ax = plt.subplots(figsize=(7.5, 5.8));

sns.heatmap(
    cm_pct,
    annot=annot,
    fmt="",
    cmap="Blues",
    xticklabels=mnb.classes_,
    yticklabels=mnb.classes_,
    vmin=0,
    vmax=100,
    cbar=True,
    cbar_kws={"label": "Porcentaje dentro del medio real (%)"},
    linewidths=0.5,
    linecolor="white",
    ax=ax
);

ax.set_xlabel("Predicción");
ax.set_ylabel("Medio real");
ax.set_title(f"Matriz de confusión - Multinomial Naive Bayes base\nAccuracy test = {acc_mnb:.3f}");

plt.xticks(rotation=45, ha="right");
plt.yticks(rotation=0);

plt.tight_layout()
plt.show()


```

La matriz de difusión presentada en la Figura \ref{fig-cm-mnb} expone en cada celda la cantidad de artículos predichos como pertenecientes al medio en la correspondiente columna, que pertenecen en la realidad al medio correspondiente a su fila. Además, se muestra a qué porcentaje de artículos dentro del medio real corresponde esa cantidad.



```{python}
#| results: asis

reporte_mnb = classification_report(
    y_test,
    y_pred_mnb,
    output_dict=True
)

tabla_mnb = (
    pd.DataFrame(reporte_mnb)
    .T
    .loc[list(mnb.classes_), ["precision", "recall", "f1-score"]]
    .rename(columns={
        "precision": "Precision",
        "recall": "Recall",
        "f1-score": "F1-score"
    })
)

tabla_mnb["Precision"] = tabla_mnb["Precision"].map(lambda x: f"{x:.3f}")
tabla_mnb["Recall"] = tabla_mnb["Recall"].map(lambda x: f"{x:.3f}")
tabla_mnb["F1-score"] = tabla_mnb["F1-score"].map(lambda x: f"{x:.3f}")

latex_mnb = tabla_mnb.to_latex(
    index=True,
    escape=True,
    caption="Precision, recall y F1 por medio (Multinomial Naive Bayes).",
    label="tab:metricas-mnb",
    column_format="lccc",
    position="H"
).replace(
    "\\begin{table}[H]",
    "\\begin{table}[H]\n\\centering"
)

print(latex_mnb)
```

La métricas presentadas en la Tabla \ref{metricas-mn} se relacionan directamente con la matriz de confusión:

- El *recall* de cada medio se lee por filas, ya que mide qué proporción de los artículos reales de ese medio fue correctamente clasificada. 
- La *precision*, en cambio, se interpreta por columnas, porque mide qué proporción de los artículos predichos como un medio realmente pertenecía a ese medio. 
- El *F1-score* resume ambas dimensiones mediante un promedio armónico, por lo que penaliza los casos en los que una de las dos métricas es alta pero la otra es muy baja.


La Figura \ref{fig-cm-mnb} muestra que el modelo Multinomial Naive Bayes base tiende a predecir una gran proporción de artículos como \text{Reuters}. Esto se observa especialmente en la fila correspondiente a \text{CNBC}: de los 773 artículos reales de este medio, solo 16 son clasificados correctamente como \text{CNBC}, mientras que 744 son clasificados erróneamente como \text{Reuters}. En términos porcentuales, esto implica que el modelo solo recupera el 2.1 % de los artículos de \text{CNBC}. Por lo tanto, aunque la *precision* para este medio es igual a 1, este valor debe interpretarse con cuidado: todos los artículos que el modelo predijo como \text{CNBC} efectivamente pertenecían a \text{CNBC}, pero el modelo casi nunca eligió esta clase. Esto explica que el *recall* de \text{CNBC} sea muy bajo, igual a 0.021, y que su *F1-score* también sea bajo, igual a 0.041.

Para \text{Reuters} ocurre lo contrario. La matriz de confusión muestra que el modelo clasifica correctamente 2769 de los 2782 artículos reales de \text{Reuters}, es decir, alcanza un *recall* de 0.995. Esto indica que casi todos los artículos de \text{Reuters} son identificados como tales. Sin embargo, la *precision* de \text{Reuter}s es menor, igual a 0.697, porque muchas predicciones de \text{Reuters} provienen en realidad de otros medios: 744 artículos de \text{CNBC} y 457 de \text{The New York Times} fueron clasificados erróneamente como \text{Reuters}. En este caso, el modelo tiene una alta capacidad para recuperar los artículos reales de \text{Reuters}, pero también asigna demasiados artículos de otras clases a esta categoría.

En el caso de \text{The New York Times}, el modelo clasifica correctamente 386 de 843 artículos reales, lo que da un *recall* de 0.458. Es decir, logra identificar algo menos de la mitad de los artículos de este medio. La *precision*, en cambio, es alta, igual a 0.937, porque la mayoría de los artículos que el modelo predice como \text{The New York Times} efectivamente pertenecen a ese medio. Su *F1-score* de 0.615 refleja este comportamiento intermedio: el modelo es relativamente confiable cuando predice esta clase, pero deja sin recuperar una proporción importante de sus artículos reales.


Mirar solamente el *accuracy* puede ser problemático. El modelo alcanza un *accuracy* de 0.721, lo que a primera vista podría sugerir un desempeño aceptable. Sin embargo, este valor está muy influido por la clase mayoritaria, \text{Reuters}, que representa cerca de dos tercios de los artículos. De hecho, un clasificador muy simple que predijera siempre \text{Reuters} ya obtendría un *accuracy* relativamente alto, cercano al peso de esa clase en el conjunto de test, aunque no fuera capaz de reconocer correctamente los otros medios. Si el desbalance entre medios fuera aún mayor, este problema se acentuaría: el *accuracy* podría seguir siendo alto incluso si el modelo ignorara casi por completo a las clases minoritarias. Por eso, en problemas con clases desbalanceadas es necesario complementar el *accuracy* con la matriz de confusión y con métricas por clase como *precision*, *recall* y *f1-score*.

## 2. Validación cruzada y búsqueda de hiperparámetros


La técnica de validación cruzada permite evaluar el desempeño del modelo de una forma más robusta que una única partición entre entrenamiento y validación. Para ello, el conjunto de entrenamiento se divide en varios pliegues o *folds*: en cada iteración, el modelo se entrena con una parte de los datos y se valida con el pliegue restante. Luego, se promedian los resultados obtenidos en las distintas iteraciones. De esta forma, se reduce la dependencia del resultado respecto de una única partición de los datos y se obtiene una estimación más estable del desempeño esperado del modelo. La cantidad de pliegues que usada en este caso es 5 y el orden de las palabras es mezclado previamente.

El hiperparámetro a tunear es *alpha*, el cual define la intensidad del suavizado aplicado por el modelo Multinomial Naive Bayes. Este suavizado permite evitar que términos que no aparecen en una clase durante el entrenamiento reciban probabilidad nula. Valores pequeños de *alpha* implican poco suavizado, por lo que el modelo queda más influido por las frecuencias observadas en los datos. En cambio, valores más grandes aplican un suavizado más fuerte, haciendo que las probabilidades estimadas dependan menos de las frecuencias observadas y sean más uniformes entre términos.

La grilla a utilizar cuenta con 9 valores: `[0.001, 0.00316, 0.01, 0.0316, 0.1, 0.316, 1, 3.16, 10]`. Estos valores fueron elegidos en escala logarítmica, ya que el efecto de *alpha* puede variar por órdenes de magnitud. Por este motivo, resulta más informativo comparar valores como `0.001`, `0.01`, `0.1`, `1` y `10` que utilizar una grilla equidistante. 

No se ha incuido en la grilla el 0, ya que equivale a no aplicar suavizado, por lo que no es un valor recomendable. En problemas de clasificación de texto, donde muchas palabras pueden aparecer pocas veces o no aparecer en alguna clase, la ausencia de suavizado puede generar probabilidades nulas y volver al modelo menos robusto.

Se ajusta un modelo Multinomial Naive Bayes para cada uno de los valores del hiperparámetro *alpha*, se comapran las métricas y se obtiene el mejor de ellos según su *accuracy*. 

```{python}
alphas = np.array([ 0.001, 0.00316, 0.01, 0.0316, 0.1, 0.316, 1, 3.16, 10]); #equivalente a alphas = np.logspace(-3, 1, 9)

# from sklearn.model_selection import StratifiedKFold # agregar a 

param_grid_mnb = {
    "alpha": alphas
};

cv = StratifiedKFold(
    n_splits=5,
    shuffle=True,
    random_state=123
);

grid_mnb = GridSearchCV(
    estimator=MultinomialNB(),
    param_grid=param_grid_mnb,
    scoring="accuracy",
    cv=cv,
    n_jobs=-1,
    return_train_score=True
);

grid_mnb.fit(X_train_model, y_train);

#print("Mejor alpha:", grid_mnb.best_params_["alpha"])
#print(f"Mejor accuracy promedio en CV: {grid_mnb.best_score_:.3f}")

#Mejor alpha: 0.0316
#Mejor accuracy promedio en CV: 0.817

```


```{python}
#| label: fig-mnb-cv
#| fig-cap: "Distribución del accuracy por valor de alpha Multinomial Naive Bayes con validación cruzada."
#| fig-align: center
#| fig-pos: "H"

# Resultados de la validación cruzada
resultados_cv = pd.DataFrame(grid_mnb.cv_results_)

# Pasamos de formato ancho a formato largo:
# una fila por combinación de alpha y split de validación cruzada.
split_cols = [
    col for col in resultados_cv.columns
    if col.startswith("split") and col.endswith("_test_score")
]

resultados_largos = resultados_cv.melt(
    id_vars=["param_alpha", "mean_test_score", "std_test_score", "rank_test_score"],
    value_vars=split_cols,
    var_name="split",
    value_name="accuracy"
)

resultados_largos["alpha"] = resultados_largos["param_alpha"].astype(float)
resultados_largos["alpha_label"] = resultados_largos["alpha"].map(lambda x: f"{x:.4g}")

# Ordenamos las etiquetas según el valor numérico de alpha
orden_alpha = (
    resultados_largos[["alpha", "alpha_label"]]
    .drop_duplicates()
    .sort_values("alpha")["alpha_label"]
    .tolist()
)


fig, ax = plt.subplots(figsize=(10, 5.8))

# Violines: distribución de accuracy entre folds para cada alpha
sns.violinplot(
    data=resultados_largos,
    x="alpha_label",
    y="accuracy",
    order=orden_alpha,
    palette="viridis",
    inner=None,
    linewidth=1.1,
    cut=0,
    ax=ax
)

# Puntos individuales: resultado de cada fold
sns.stripplot(
    data=resultados_largos,
    x="alpha_label",
    y="accuracy",
    order=orden_alpha,
    color="black",
    size=4,
    alpha=0.65,
    jitter=0.12,
    ax=ax
)

# Promedio de accuracy para cada alpha
sns.pointplot(
    data=resultados_largos,
    x="alpha_label",
    y="accuracy",
    order=orden_alpha,
    estimator="mean",
    errorbar=None,
    color="white",
    markers="D",
    linestyles="",
    markersize=4,
    ax=ax
)


# Formato general
ax.set_xlabel("alpha")
ax.set_ylabel("Accuracy")
ax.set_title("Accuracy según alpha en Multinomial Naive Bayes CV")

ax.tick_params(axis="x")
ax.grid(axis="y", linestyle="--", alpha=0.35)
ax.set_axisbelow(True)


plt.tight_layout()
plt.show()
```

En la Figura \ref{fig-mnb-cv} se observa la distribución del *accuracy* del modelo para cada valor del hiperparámetro *alpha*. Los puntos negros representan el *accuracy* obtenido en cada uno de los pliegues, mientras que el rombo blanco muestra la media de estos en cada caso.


```{python}
#| results: asis

tabla_resultados_mnb = (
    resultados_cv[["param_alpha", "mean_test_score", "std_test_score", "rank_test_score"]]
    .rename(columns={
        "param_alpha": "Alpha",
        "mean_test_score": "Accuracy promedio",
        "std_test_score": "Desvío estándar",
        "rank_test_score": "Ranking"
    })
    .sort_values("Ranking")
)

# Redondeamos para que la tabla quede más limpia
tabla_resultados_mnb["Alpha"] = tabla_resultados_mnb["Alpha"].map(lambda x: f"{x:.5g}")
#tabla_resultados_mnb["Alpha"] = tabla_resultados_mnb["Alpha"].astype(float)
tabla_resultados_mnb["Accuracy promedio"] = tabla_resultados_mnb["Accuracy promedio"].map(lambda x: f"{x:.3f}")
tabla_resultados_mnb["Desvío estándar"] = tabla_resultados_mnb["Desvío estándar"].map(lambda x: f"{x:.3f}")

print(
    tabla_resultados_mnb.to_latex(
        index=False,
        caption="Resultados de validación cruzada para distintos valores de alpha en Multinomial Naive Bayes.",
        label="tab:cv-alpha-mnb",
        position="H",
        escape=False
    ).replace(
        "\\begin{table}[H]",
        "\\begin{table}[H]\n\\centering"
    )
)
```

El mejor valor de *alpha* según el *accuracy* promedio es 0.0316, con un accuracy de 0.81. Aún así el valor 0.1 se desempeña prácticamente de la misma manera y cuenta con un desvío estándar menor, por lo que en algún caso podría preferirse este valor. Igualmente, todos los valores obtenidos para el desvío entándar son moderados, por lo que ambos valores son recomendables.

En general, se observa que valores bajos de *alpha* tienden a desempeñarse mejor. Valores mayores que $0.1$ cuentan con un *accuracy* cada vez menor. Esto sugiere que un suavizado demasiado fuerte vuelve al modelo menos discriminativo. Al aumentar demasiado *alpha*, las probabilidades de las palabras se suavizan tanto que se pierde parte de la información útil que aportaban las frecuencias observadas en los textos.

**Mejor modelo:**

Con el valor de *alpha* más óptimo (0.0316) se obtiene 10% más de aciertos en validación cruzada que con el parámetro por defecto que usa la función _MultinomialNB_ (*alpha* = 1), el cual se usó anteriormente como primera aproximación al modelo Multinomial Naive Bayes, devolviendo los resultados mostrados en la Figura \ref{fig-cm-mnb}.

## 3. Entrenamiento final con el mejor modelo
Elija el mejor modelo (mejores parámetros) y vuelva a entrenar sobre todo el conjunto de entrenamiento disponible (sin quitar datos para validación).
Reporte el valor final de las métricas y la matriz de confusión.
Discuta las limitaciones de utilizar un modelo basado en *bag of words* o TF-IDF para el análisis de texto.

Una vez seleccionado el mejor valor del hiperparámetro mediante validación cruzada, se entrena el modelo final utilizando el conjunto completo de entrenamiento. En este caso, el mejor valor encontrado fue `alpha = 0.0316`, que obtuvo el mayor accuracy promedio en validación cruzada.

Luego, el modelo final se evalúa sobre el conjunto de test, que no fue utilizado durante el ajuste de hiperparámetros. Esto permite obtener una estimación más realista de su desempeño predictivo sobre datos no observados.

```{python}
mejor_alpha = grid_mnb.best_params_["alpha"];

# Entrenamiento final del modelo con el mejor alpha
modelo_final_mnb = MultinomialNB(alpha=mejor_alpha);

modelo_final_mnb.fit(X_train_model, y_train);

# Predicciones sobre el conjunto de test
y_pred_final_mnb = modelo_final_mnb.predict(X_test_model);

# Accuracy en test
accuracy_final_mnb = accuracy_score(y_test, y_pred_final_mnb);

#print(f"Mejor alpha: {mejor_alpha:.4g}")
#print(f"Accuracy en test del modelo final: {accuracy_final_mnb:.3f}")
```


```{python}
#| label: fig-mnb-matriz
#| fig-cap: "Matriz de confusión normalizada por medio real para el modelo final Multinomial Naive Bayes."
#| fig-align: center
#| fig-pos: "H"


# Matriz de confusión en cantidades absolutas
cm_abs = confusion_matrix(
    y_test,
    y_pred_final_mnb,
    labels=modelo_final_mnb.classes_
);

# Normalización por fila: porcentaje sobre el total real de cada medio
cm_pct = cm_abs / cm_abs.sum(axis=1, keepdims=True) * 100;

# Texto de cada celda: cantidad absoluta y porcentaje
annot = np.empty_like(cm_abs).astype(str);

for i in range(cm_abs.shape[0]):
    for j in range(cm_abs.shape[1]):
        annot[i, j] = f"{cm_abs[i, j]}\n({cm_pct[i, j]:.1f}%)"

fig, ax = plt.subplots(figsize=(7.5, 5.8));

sns.heatmap(
    cm_pct,
    annot=annot,
    fmt="",
    cmap="Blues",
    xticklabels=modelo_final_mnb.classes_,
    yticklabels=modelo_final_mnb.classes_,
    vmin=0,
    vmax=100,
    cbar_kws={"label": "Porcentaje dentro del medio real (%)"},
    linewidths=0.5,
    linecolor="white",
    ax=ax
);

ax.set_xlabel("Predicción");
ax.set_ylabel("Medio real");
ax.set_title(f"Matriz de confusión \nalpha = {mejor_alpha:.4g} - Accuracy test = {accuracy_final_mnb:.3f}");

plt.xticks(rotation=45, ha="right");
plt.yticks(rotation=0);

plt.tight_layout()
plt.show()
```

El modelo final Multinomial Naive Bayes, entrenado con `alpha = 0.0316`, alcanza un *accuracy* en test cercano a 0.82. La Figura \ref{fig-mnb-matriz} muestra una notable mejoría en la clasificación para los medios \text{CNBC} y \text{The New York Times} en comparación con el modelo ajustado anteriormente. Los artículos pertenecientes a \text{Reuters} siguen siendo los que clasifica mejor, aunque el porcentaje de predicciones correctas es menor para este medio en relación al modelo anterior. El medio \text{CNBC} continúa siendo el que en terminos relativos predice peor: se predicen erroneamente como pertenecientes a \text{Reuters} gran parte de sus artículos. Esto puede deberse al desbalance de datos, ya que este medio es el que cuenta con la menor cantidad de artículos de entre los tres, pero también a que una parte importante de los textos de \text{CNBC} comparte características léxicas o temáticas con \text{Reuters} según la representación TF-IDF utilizada.

```{python}
#| results: asis

reporte_final_mnb = classification_report(
    y_test,
    y_pred_final_mnb,
    output_dict=True
)

tabla_metricas_final_mnb = (
    pd.DataFrame(reporte_final_mnb)
    .T
    .loc[modelo_final_mnb.classes_, ["precision", "recall", "f1-score"]]
    .rename(columns={
        "precision": "Precision",
        "recall": "Recall",
        "f1-score": "F1-score"
    })
)

tabla_metricas_final_mnb["Precision"] = tabla_metricas_final_mnb["Precision"].map(lambda x: f"{x:.3f}")
tabla_metricas_final_mnb["Recall"] = tabla_metricas_final_mnb["Recall"].map(lambda x: f"{x:.3f}")
tabla_metricas_final_mnb["F1-score"] = tabla_metricas_final_mnb["F1-score"].map(lambda x: f"{x:.3f}")

print(
    tabla_metricas_final_mnb.to_latex(
        caption="Precision, recall y F1 por medio para el modelo final Multinomial Naive Bayes.",
        label="tab:metricas-final-mnb",
        position="H",
        escape=False
    ).replace(
        "\\begin{table}[H]",
        "\\begin{table}[H]\n\\centering"
    )
)
```

La Tabla \ref{metricas-final-mnb} complementa la lectura de la matriz de confusión al resumir, para cada medio, los valores de *precision*, *recall* y *F1-score*. En el caso de \text{CNBC}, el modelo obtiene una *precision* de 0.699 y un *recall* de 0.511. Esto significa que, de todos los artículos que el modelo predice como \text{CNBC}, aproximadamente el 69.9 % efectivamente pertenece a ese medio; y que, de todos los artículos reales de \text{CNBC}, el modelo logra recuperar el 51.1 %. Este último valor coincide con la diagonal de la matriz de confusión normalizada de la Figura \ref{fig-mnb-matriz}, donde se observa que 395 de los 773 artículos reales de \text{CNBC} fueron clasificados correctamente. El *F1-score* de 0.590 refleja este desempeño todavía limitado, aunque considerablemente mejor que el del modelo base.

Para \text{Reuters}, el modelo alcanza los mejores resultados generales: una *precision* de 0.871, un *recall* de 0.897 y un *F1-score* de 0.884. La matriz de confusión muestra que el 89.7 % de los artículos reales de Reuters fueron correctamente clasificados, lo que coincide con su *recall*. Además, la *precision* elevada indica que la mayoría de los artículos predichos como \text{Reuters} efectivamente pertenecen a ese medio. Aun así, esta clase sigue recibiendo una cantidad importante de errores provenientes de \text{CNBC}: 285 artículos reales de \text{CNBC} fueron clasificados como \text{Reuters}.

En el caso de \text{The New York Times}, el *recall* es alto, igual a 0.847, lo que indica que el modelo logra identificar correctamente la mayor parte de los artículos reales de este medio. Esto también se observa en la Figura \ref{fig-mnb-matriz}, donde 714 de 843 artículos reales aparecen en la diagonal. Su *precision*, de 0.738, es menor que su *recall*, lo que implica que dentro de los artículos predichos como \text{The New York Times\text{ hay una proporción no despreciable de textos que en realidad pertenecen a otros medios. El *F1-score* de 0.789 resume este balance entre una buena capacidad de recuperación y una *precision* más moderada.

En conjunto, la Tabla\ref{metricas-final-mnb} confirma la mejora del modelo final respecto del Multinomial Naive Bayes base. El ajuste de *alpha* permite que el modelo deje de concentrar casi todas sus predicciones en Reuters y mejore especialmente el *recall* de CNBC y The New York Times. Sin embargo, las métricas también muestran que la clasificación no es igualmente buena para todos los medios: Reuters sigue siendo la clase mejor resuelta, mientras que CNBC continúa siendo la más difícil, tanto por su menor *recall* como por su *F1-score* más bajo.

En conclusión, al comparar comparar con el modelo Multinomial Naive Bayes base, el ajuste del hiperparámetro *alpha* permite mejorar el desempeño general y, en particular, la recuperación de las clases minoritarias. Sin embargo, persisten confusiones relevantes entre \text{CNBC} y \text{Reuters}, lo que sugiere que estas clases son más difíciles de separar mediante este modelo.

**limitaciones de usar Bag of Words o TF-IDF:**

Una limitación importante de los modelos basados en Bag of Words o TF-IDF es que representan los textos como bolsas de términos, por lo que ignoran el orden de las palabras y buena parte del contexto lingüístico. Dos frases con sentidos distintos pueden terminar teniendo representaciones muy parecidas si contienen las mismas palabras. Por ejemplo, estas técnicas no capturan bien negaciones, relaciones sintácticas, ironía, tono ni dependencias de largo alcance entre términos.

Otra limitación es que estas representaciones trabajan principalmente con coincidencias superficiales de vocabulario. En consecuencia, pueden asignar mucho peso a marcas de estilo, formato o edición, como firmas, fórmulas periodísticas, nombres propios recurrentes o expresiones típicas de un medio, aunque esas palabras no representen necesariamente diferencias temáticas profundas. En este trabajo, esto es especialmente relevante porque los medios pueden distinguirse no solo por los temas que cubren, sino también por convenciones de redacción, plantillas editoriales y formas de citar fuentes.

Además, tanto Bag of Words como TF-IDF generan matrices de muy alta dimensionalidad y muy dispersas: cada palabra o n-grama se convierte en una variable, pero cada artículo utiliza solo una pequeña parte del vocabulario total. Esto puede volver al modelo sensible a términos raros, errores de limpieza, variantes morfológicas o expresiones poco frecuentes. Aunque TF-IDF reduce el peso de palabras demasiado comunes y destaca términos más distintivos, sigue sin comprender el significado semántico de las palabras ni la similitud entre términos relacionados.

Por estas razones, los resultados obtenidos deben interpretarse como la capacidad del modelo para explotar patrones léxicos presentes en los textos, más que como una comprensión profunda del contenido periodístico. Técnicas más avanzadas, como embeddings de palabras o modelos basados en transformers, podrían capturar relaciones semánticas y contextuales más ricas, aunque a costa de mayor complejidad computacional y menor interpretabilidad directa.




## 4. Modelo alternativo
Evalúe al menos un modelo más (dentro de scikit-learn) aparte de *Multinomial Naive Bayes* para clasificar el texto utilizando las mismas *features* de texto.
Explique brevemente cómo funciona y compare los resultados con el anterior.

```{python}
# Regresión logística multinomial con regularización L2 (por defecto en sklearn),
# entrenada sobre las mismas features TF-IDF
logreg = LogisticRegression(max_iter=1000, C=1.0);
logreg.fit(X_train_model, y_train);

y_pred_lr = logreg.predict(X_test_model);

acc_lr = accuracy_score(y_test, y_pred_lr);
#print(f"Accuracy en test: {acc_lr:.3f}")
```

```{python}
#| label: fig-cm-logreg
#| fig-cap: "Matriz de confusión de la regresión logística multinomial sobre el conjunto de test."
#| fig-align: center
#| fig-pos: "H"

# Matriz de confusión en cantidades absolutas
cm_abs_lr = confusion_matrix(
    y_test,
    y_pred_lr,
    labels=logreg.classes_
);

# Normalización por fila:
# cada fila representa el 100% de los artículos reales de ese medio
cm_pct_lr = cm_abs_lr / cm_abs_lr.sum(axis=1, keepdims=True) * 100;

# Texto de cada celda: cantidad absoluta y porcentaje
annot_lr = np.empty_like(cm_abs_lr).astype(str);

for i in range(cm_abs_lr.shape[0]):
    for j in range(cm_abs_lr.shape[1]):
        annot_lr[i, j] = f"{cm_abs_lr[i, j]}\n({cm_pct_lr[i, j]:.1f}%)";

fig, ax = plt.subplots(figsize=(7.5, 5.8));

sns.heatmap(
    cm_pct_lr,
    annot=annot_lr,
    fmt="",
    cmap="Blues",
    xticklabels=logreg.classes_,
    yticklabels=logreg.classes_,
    vmin=0,
    vmax=100,
    cbar=True,
    cbar_kws={"label": "Porcentaje dentro del medio real (%)"},
    linewidths=0.5,
    linecolor="white",
    ax=ax
);

ax.set_xlabel("Predicción");
ax.set_ylabel("Medio real");
ax.set_title(f"Matriz de confusión - Regresión logística multinomial \nAccuracy test = {acc_lr:.3f}");

plt.xticks(rotation=45, ha="right");
plt.yticks(rotation=0);

plt.tight_layout()
plt.show()
```

```{python}
#| results: asis

reporte_lr = classification_report(
    y_test,
    y_pred_lr,
    output_dict=True
)

tabla_lr = (
    pd.DataFrame(reporte_lr)
    .T
    .loc[list(logreg.classes_), ["precision", "recall", "f1-score"]]
    .rename(columns={
        "precision": "Precision",
        "recall": "Recall",
        "f1-score": "F1-score"
    })
)

tabla_lr.index.name = "Medio"

tabla_lr["Precision"] = tabla_lr["Precision"].map(lambda x: f"{x:.3f}")
tabla_lr["Recall"] = tabla_lr["Recall"].map(lambda x: f"{x:.3f}")
tabla_lr["F1-score"] = tabla_lr["F1-score"].map(lambda x: f"{x:.3f}")

print(
    tabla_lr.to_latex(
        index=True,
        escape=True,
        caption="Precision, recall y F1 por medio (regresión logística multinomial).",
        label="tab:metricas-logreg",
        column_format="lccc",
        position="H"
    ).replace(
        "\\begin{table}[H]",
        "\\begin{table}[H]\n\\centering"
    )
)
```

Una ventaja de la regresión logística frente al Naive Bayes es que sus coeficientes son fácilmente interpretables. La matriz `logreg.coef_` tiene una fila por medio y una columna por palabra, y el coeficiente asociado a cada palabra indica cuánto empuja su presencia la predicción hacia ese medio. En la Figura \ref{fig-coef-logreg} se muestran, para cada medio, las palabras con mayor coeficiente positivo, es decir, las más relevantes a la hora de clasificar.

```{python}
#| label: fig-coef-logreg
#| fig-cap: "Palabras con mayor coeficiente positivo para cada medio en la regresión logística multinomial."
#| fig-align: center
#| fig-pos: "H"

nombres = np.array(tfidf_model.get_feature_names_out())
colores = {
    "Reuters": "tab:orange",
    "CNBC": "tab:blue",
    "The New York Times": "tab:green"
}
k = 12

fig, axs = plt.subplots(1, 3, figsize=(13, 5))
for ax, medio in zip(axs, logreg.classes_):
    i = list(logreg.classes_).index(medio)
    coef = logreg.coef_[i]
    top = np.argsort(coef)[::-1][:k]
    ax.barh(nombres[top][::-1], coef[top][::-1], color=colores[medio])
    ax.set_title(medio, fontsize=11)
    ax.tick_params(axis="y", labelsize=9)
    ax.set_xlabel("Coeficiente")

plt.tight_layout()
plt.show()
```

Lo que más llama la atención es que, **aun habiendo quitado las stop words y los nombres de los medios**, casi ninguna de las palabras más influyentes es temática: el modelo se apoya sobre todo en marcas de estilo y de formato. \texttt{Reuters} se reconoce por el formato de sus cables (*editing*, *reporting*, *text*, *source*, *writing*, *newsroom*) y por rastros de su estructura interna (*corp*, *bengaluru*, sede de una de sus redacciones). \texttt{The New York Times} se identifica por sus honoríficos característicos (*mr*, *ms*, *dr*) y por vocabulario editorial (*article*, *editor*, *newsletter*). \texttt{CNBC}, en cambio, aparece asociado a contracciones del inglés informal (*don*, *ve*, *ll*, *didn*, fragmentos de "don't", "I've", etc.) y a algunos nombres y temas propios (*ceo*, *trump*, *china*, *cramer*, conductor del canal).

El resultado es revelador: eliminar las referencias directas al medio no alcanzó para forzar al modelo a fijarse en el contenido. La señal más fuerte sigue proviniendo del estilo y el formato (plantillas de redacción, honoríficos, contracciones) e incluso de entidades muy asociadas a cada medio (su sede, sus conductores), además de artefactos de la propia limpieza, como las contracciones partidas al eliminar los apóstrofes. En otras palabras, el clasificador aprende a distinguir *cómo* y *quién* escribe antes que *de qué* se habla. Para medir la verdadera capacidad de diferenciar medios por su temática habría que seguir limpiando estas marcas, y es esperable que la tarea se vuelva entonces bastante más difícil.

Por último, vale la pena ver el rol de la **regularización**, que es lo que controla cuánto peso puede tomar cada palabra. La Figura \ref{fig-path} ilustra, sobre un vocabulario reducido a 400 términos para que el cálculo sea rápido, cómo evolucionan los coeficientes de algunas palabras a medida que se afloja la penalización (es decir, al aumentar $C$, el inverso de la fuerza de regularización), comparando las penalizaciones L2 (*Ridge*) y L1 (*Lasso*).

```{python}
#| label: fig-path
#| fig-cap: "Trayectoria de los coeficientes de algunas palabras al variar la regularización, con penalización L2 (Ridge) y L1 (Lasso), sobre un vocabulario reducido."
#| fig-align: center
#| fig-pos: "H"

tfidf_path = TfidfVectorizer(stop_words=stopwords_modelo, max_features=400)
X_path = tfidf_path.fit_transform(X_train)
vocab = {w: i for i, w in enumerate(tfidf_path.get_feature_names_out())}

seguir = {
    "editing": "Reuters", "reporting": "Reuters",
    "mr": "The New York Times", "ms": "The New York Times",
    "trump": "CNBC", "china": "CNBC"
}
seguir = {w: m for w, m in seguir.items() if w in vocab}

Cs = np.logspace(-2, 2, 9)
paths = {pen: {w: [] for w in seguir} for pen in ["l2", "l1"]}
for pen in ["l2", "l1"]:
    for C in Cs:
        ovr = OneVsRestClassifier(
            LogisticRegression(penalty=pen, solver="liblinear", C=C, max_iter=2000)
        ).fit(X_path, y_train)
        clases = list(ovr.classes_)
        for w, medio in seguir.items():
            est = ovr.estimators_[clases.index(medio)]
            paths[pen][w].append(est.coef_[0, vocab[w]])

fig, axs = plt.subplots(1, 2, figsize=(11, 4.5), sharey=True)
for ax, pen, titulo in zip(axs, ["l2", "l1"], ["Ridge (L2)", "Lasso (L1)"]):
    for w, medio in seguir.items():
        ax.plot(Cs, paths[pen][w], marker="o", ms=3, color=colores[medio])
        ax.annotate(
            w, (Cs[-1], paths[pen][w][-1]),
            fontsize=8, color=colores[medio],
            xytext=(4, 0), textcoords="offset points"
        )
    ax.set_xscale("log")
    ax.set_title(titulo)
    ax.set_xlabel("C (inverso de la regularización)")
    ax.axhline(0, color="gray", lw=0.6, ls="--")
axs[0].set_ylabel("Coeficiente")
plt.tight_layout()
plt.show()
```

En ambos casos, cuando la regularización es fuerte (a la izquierda, con $C$ pequeño) todos los coeficientes se encogen hacia cero y el modelo resulta más simple; al aflojarla (hacia la derecha) cada palabra puede pesar más. La diferencia está en *cómo* lo hacen: con **L2 (Ridge)** los coeficientes se reducen de forma suave pero rara vez llegan a cero, mientras que con **L1 (Lasso)** las palabras menos informativas se "apagan", es decir, su coeficiente pasa a ser exactamente cero (se aprecia, por ejemplo, en *trump* y *china* bajo regularización fuerte), lo que produce un modelo más ralo que selecciona de forma automática las palabras más útiles.

## 5. Cambio de medio de prensa
Evalúe el problema cambiando al menos un medio de prensa.
En particular, observe el (des)balance de datos y los problemas que pueda generar, así como cualquier indicio que pueda ver en el mapeo previo con PCA.
Puede ser útil comentar acerca de técnicas como sobre-muestreo y submuestreo; no es necesario implementarlas.

```{python}
# TODO: Seleccione una combinación diferente de tres medios de prensa (cambiando al menos uno)
# y repita el proceso de entrenamiento y evaluación.

# Analice el balance de clases en este nuevo conjunto de datos
# ...
```

## 6. Técnica alternativa de extracción de features
Busque información sobre al menos una técnica alternativa de extraer *features* de texto.
Explique brevemente cómo funciona y qué tipo de diferencias esperaría en los resultados.
No se espera que implemente nada en esta parte.

*TODO: Escriba su análisis en el informe.*

## 7. Opcional: Clasificación a nivel de título
Repita la clasificación con los tres medios de prensa originales, pero esta vez clasificando a nivel de **título** en lugar de artículo completo.

```{python}
# Opcional: Repita el pipeline de clasificación utilizando el título del artículo
# en lugar del cuerpo del texto.

```

